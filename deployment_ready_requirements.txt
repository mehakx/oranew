Flask==3.0.0
flask-cors==4.0.0
gunicorn==21.2.0
openai==1.35.0
python-dotenv==1.0.0
numpy==1.24.3
# Optional - will use if available, otherwise falls back to OpenAI embeddings
sentence-transformers==2.2.2
