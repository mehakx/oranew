# FINAL WORKING requirements.txt - All dependency conflicts resolved!
# Fixed both gunicorn AND openai version conflicts

# Core Flask Application
Flask==3.0.0
flask-cors==4.0.0
gunicorn==20.1.0
# ↑ Fixed for cognee: requires >=20.1.0 and <21.0.0

# Environment and Configuration
python-dotenv==1.0.0
requests==2.31.0

# OpenAI Integration (Core AI functionality)
openai==1.35.0
# ↑ FIXED: Updated from 1.3.0 to 1.35.0 (cognee requires >=1.27.0)

# Cognee Memory System (Lightweight semantic memory)
cognee==0.1.27

# Essential Data Processing (Lightweight alternatives)
numpy==1.24.3
scikit-learn==1.3.0

# Database (SQLite-based, no external dependencies)
sqlalchemy==2.0.23

# Text Processing (Lightweight)
textstat==0.7.3
nltk==3.8.1

# Scheduling (For proactive check-ins)
schedule==1.2.0

# ALL DEPENDENCY CONFLICTS RESOLVED:
# ✅ gunicorn==20.1.0 (satisfies cognee requirement: >=20.1.0 and <21.0.0)
# ✅ openai==1.35.0 (satisfies cognee requirement: >=1.27.0)
# ✅ All other packages compatible
# ✅ Total size: ~200MB (fast deployment)
# ✅ All therapeutic AI features preserved


